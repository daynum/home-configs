#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;

#define mp make_pair
#define rall(x) (x).rbegin(), (x).rend()
#define all(x) (x).begin(), (x).end()
#define int int64_t
#define watch(x) cout << (#x) << " is " << (x) << endl

const int32_t MAX = 1e9 + 7;
typedef vector<int> vi;
typedef vector<int64_t> vll;
class Solve
{
public:
    void Solver()
    {
    }

private:
    void showVectorResult(vector<int> &res)
    {
        for (int a : res)
            cout << a << " ";
    }
};

int32_t main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.precision(10);
    cout << fixed;
    // problem code starts here
    Solve solver = Solve();
	$0;
    solver.Solver();
    return 0;
}